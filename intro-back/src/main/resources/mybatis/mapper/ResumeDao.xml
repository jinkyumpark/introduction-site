<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jinkyumpark.introback.resume.ResumeDao">

    <resultMap id="resumeMap" type="java.util.HashMap"></resultMap>

    <select id="getHomeProfile"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
    >
        {
            CALL getHomeProfile(
                #{id},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=resumeMap
                }
            )
        }
    </select>

    <select id="getProfile"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            >
        {
            CALL getProfile(
                #{id},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=resumeMap
                }
            )
        }
    </select>

    <select id="getLanguages"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            >
        {
            CALL getLanguages(
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=resumeMap
                }
            )
        }
    </select>

    <select id="getActivities"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
    >
        {
            CALL getActivities(
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=resumeMap
                }
            )
        }
    </select>

    <select id="getTechHeader"
            statementType="CALLABLE"
            parameterType="java.util.HashMap">
        {
            CALL getTechHeader(
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=resumeMap
                }
            )
        }
    </select>

    <select id="getTechList"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
        >
            {
                CALL getTechList(
                    #{page},
                    #{
                        ref_cursor,
                        mode=OUT,
                        jdbcType=CURSOR,
                        javaType=ResultSet,
                        resultMap=resumeMap
                    }
                )
            }
    </select>

    <select id="getTechListSummary"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            >
        {
            CALL getTechListSummary(
                #{num},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=resumeMap
                }
            )
        }
    </select>

    <select id="getIntroduction"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            >
        {
            CALL getIntroduction(
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=resumeMap
                }
            )
        }
    </select>

    <select id="getInterview"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            >
        {
            CALL getInterview(
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=resumeMap
                }
            )
        }
    </select>

    <select id="getTechPost"
            statementType="CALLABLE"
            parameterType="java.util.HashMap">
        {
            CALL getTechPost(
                #{num},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=resumeMap
                }
            )
        }
    </select>
</mapper>