<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jinkyumpark.introback.portfolio.PortfolioDao">

    <resultMap id="portfolioMap" type="java.util.HashMap"></resultMap>

    <select id="getPortfolioList"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
    >
        {
            CALL getPortfolioList(
                #{page},
                #{fetch_num},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=portfolioMap
                }
            )
        }
    </select>

    <select id="getPortfolioLength"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            resultType="_int"
            >
        {
            CALL getPortfolioLength(
                #{
                    length,
                    mode=OUT,
                    jdbcType=INTEGER
                }
            )
        }
    </select>

    <select id="getPortfolioDetail"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            >
        {
            CALL getPortfolioDetail(
                #{num},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=portfolioMap
                }
            )
        }
    </select>

    <select id="getPortfolioDetailFunction"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            >
        {
            CALL getPortfolioDetailFunction(
                #{num},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=portfolioMap
                }
            )
        }
    </select>

    <select id="getPortfolioDetailTech"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            >
        {
            CALL getPortfolioDetailTech(
                #{num},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=portfolioMap
                }
            )
        }
    </select>

    <select id="getPortfolioDetailDb"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            >
        {
            CALL getPortfolioDetailDb(
                #{num},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=portfolioMap
                }
            )
        }
    </select>

    <select id="getPortfolioDetailExplanation"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            >
        {
            CALL getPortfolioDetailExplanation(
                #{num},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=portfolioMap
                }
            )
        }
    </select>

    <select id="getPortfolioDetailExplanationPost"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
    >
        {
            CALL getPortfolioDetailExplanationPost(
                #{explanation_num},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=portfolioMap
                }
            )
        }
    </select>

    <select id="getPortfolioDetailHistory"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            >
        {
            CALL getPortfolioDetailHistory(
                #{num},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=portfolioMap
                }
            )
        }
    </select>

    <select id="getPortfolioDetailReview"
            statementType="CALLABLE"
            parameterType="java.util.HashMap"
            >
        {
            CALL getPortfolioDetailReview(
                #{num},
                #{
                    ref_cursor,
                    mode=OUT,
                    jdbcType=CURSOR,
                    javaType=ResultSet,
                    resultMap=portfolioMap
                }
            )
        }
    </select>

</mapper>